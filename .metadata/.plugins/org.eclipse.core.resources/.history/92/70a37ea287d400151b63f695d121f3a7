// Meysam Aghighi

//#include <cstdio>
#include <algorithm>
#include <iostream>
using namespace std;

int T,kk,n,best,j;
int x[100],y[100];
int minx[100],miny[100],maxx[100],mxy[100];

void solve(int i, int k, int cur){
    if (k<=kk && cur<=best){
        if (i == n){
            best = min(best,cur);
        }
        else{
            for (int j=0; j<=k; ++j){
                int ominx=minx[j],ominy=miny[j],omaxx=maxx[j],omxy=mxy[j];
                minx[j]=min(minx[j],x[i]);
                miny[j]=min(miny[j],y[i]);
                maxx[j]=max(maxx[j],x[i]);
                mxy[j]=max(mxy[j],y[i]);
                solve(i+1,max(k,j+1),max(cur,max(maxx[j]-minx[j],mxy[j]-miny[j])));
                minx[j]=ominx,miny[j]=ominy,maxx[j]=omaxx,mxy[j]=omxy;
            }
        }
    }
}

int main(){
    cin >> T;
    for (int ii=0; ii<T; ii++){
        cin >> n >> kk;
        for (int i=0; i<n; i++)
            cin >> x[i] >> y[i];
        for (int i=0; i<100; i++)
            minx[i] = miny[i] = 100000,
            maxx[i] = mxy[i] = -1;
        best = 100000;
        solve(0,0,0);
        cout << "Case #" << ii+1 << ": " << best << endl;
//        printf("Case #%d: %d\n",ii+1,best);
    }
    return 0;
}
